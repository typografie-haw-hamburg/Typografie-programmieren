//@includepath "~/Documents/;%USERPROFILE%Documents";//@include "basiljs/basil.js";function draw() {//1. Dokumentgrundlagen b.clear (b.doc() );    b.units (b.MM);    b.colorMode(b.CMYK);    //Farbvariablen    var cyan = b.color(100,0,0,0);    var magenta = b.color(0,100,0,0);    var yellow = b.color (0,0,100,0);    var black = b.color (0,0,0,100);    var violet = b.color (90,80,0,0);    var lightblue = b.color (35,7,0,0);    var orange = b.color (0, 70, 100, 0);    var green = b.color(40,0,95,0);    var red = b.color (0,100,100,0);    var grey = b.color (0,0,0,10);    //Seiteneinteilung      //Seite halbiert      var halfwidth = b.width/2;      var halfheight = b.height/2;            //Obenlinks            var topleftx = 0;            var toplefty = 0;            //Obenrechts            var toprightx = b.width/2;            var toprighty = 0;            //Untenlinks            var downleftx = 0;            var downlefty = b.height/2;            //unten rechts            var downrightx = b.width/2;            var downrighty = b.height/2;      //Seite 3/4      var threeQuarterWidth = 3 * b.width/4;      var threeQuarterHeight = 3 * b.height/4;      //Seite 1/4      var oneQuarterWidth = b.width/4;      var oneQuarterHeight = b.height/4;    //2. Emotionen abfragen / FAKTOR: STRESS    var gestresst = prompt("Bist du gestresst?", "ja, ein bisschen, nein");                //JA            if (gestresst == "ja"){              b.noFill();              b.strokeWeight(3);                //Größe des Rechtecks                var rectwidth = 5;                var rectheight = 5;                //Position                var rectPosX = 5;                var rectPosY = 5;                //Anzahl der Rechtecke                var rectnumberx = 60;//von oben nach unten                var rectnumbery = 40;//von links nach rechts                for ( var l = 0; l < rectnumberx; l++) {                  for ( var m = 0; m < rectnumbery; m++) {                   var rec1 = b.rect(  m*rectPosX,  l*rectPosY, rectwidth, rectheight);                  b.blendMode (rec1, BlendMode.SCREEN);                 }                }              }             // EIN BISSCHEN            if (gestresst == "ein bisschen"){                b.noFill();                b.strokeWeight(3);                  //Größe des Rechtecks                  var rectwidth = 3;                  var rectheight = 5;                  //Position                  var rectPosX = 5;                  var rectPosY = 5;                  //Anzahl der Rechtecke                  var rectnumberx = 40;//von oben nach unten                  var rectnumbery = 25;//von links nach rechts                  for ( var l = 0; l < rectnumberx; l++) {                    for ( var m = 0; m < rectnumbery; m++) {                     var rec2 = b.rect(  m*rectPosX,  l*rectPosY, rectwidth, rectheight);                  b.blendMode (rec2, BlendMode.SCREEN);                   }                  }          }          //NEIN          if (gestresst == "nein"){                  b.noFill();              b.strokeWeight(3);                //Größe des Rechtecks                var rectwidth = 1;                var rectheight = 1;                //Position                var rectPosX = 5;                var rectPosY = 5;                //Anzahl der Rechtecke                var rectnumberx = 20;//von oben nach unten                var rectnumbery = 20;//von links nach rechts                for ( var l = 0; l < rectnumberx; l++) {                  for ( var m = 0; m < rectnumbery; m++) {                  var rec3 = b.rect(  m*rectPosX,  l*rectPosY, rectwidth, rectheight);                  b.blendMode (rec3, BlendMode.SCREEN);                 }                }          }          //3. Emotionen abfragen / FAKTOR: FREUDE          var freude = prompt("Freust du dich?", "ja, ein bisschen, nein");                                        //JA                        if (freude == "ja"){                          b.fill(yellow);                          b.noStroke();                                          //Position                            var ellPosX = oneQuarterWidth;                            var ellPosY = oneQuarterHeight;                            //Anzahl der Kreise                            var ellnumberx = 10;//von oben nach unten                            var ellnumbery = 15;//von links nach rechts                            for ( var l = 0; l < ellnumberx; l++) {                              for ( var m = 0; m < ellnumbery; m++) {                                            //Größe                                var ellrandom = b.random (20,40)                               var ell1 = b.ellipse(  m * 20 + (ellPosX + b.random(0,30)),  l * 20 + (ellPosY + b.random(0,30)), ellrandom, ellrandom);                                b.blendMode (ell1, BlendMode.SCREEN);                             }                            }                          }                         // EIN BISSCHEN                        if (freude == "ein bisschen"){                          b.fill(yellow);                          b.noStroke();                            //Größe                            var ellradius = 20                            //Position                            var ellPosX = 10;                            var ellPosY = 10;                            //Anzahl der Kreise                            var ellnumberx = 10;//von oben nach unten                            var ellnumbery = 8;//von links nach rechts                            for ( var l = 0; l < ellnumberx; l++) {                              for ( var m = 0; m < ellnumbery; m++) {                               var ell2 = b.ellipse(  m * 20 +(ellPosX + b.random(0,20)),  l * 20 +(ellPosY + b.random(0,20)), ellradius, ellradius);                                b.blendMode (ell2, BlendMode.SCREEN);                             }                            }                      }                      //NEIN                      if (freude == "nein"){                          b.fill(yellow);                          b.noStroke();                            //Größe                            var ellradius = 10                            //Position                            var ellPosX = 5;                            var ellPosY = 5;                            //Anzahl der Kreise                            var ellnumberx = 20;//von oben nach unten                            var ellnumbery = 20;//von links nach rechts                            for ( var l = 0; l < ellnumberx; l++) {                              for ( var m = 0; m < ellnumbery; m++) {                               var ell3 = b.ellipse( m * 20 ( + ellPosX),  l * 20 + (3 * b.height/4), ellradius, ellradius);                                b.blendMode (ell3, BlendMode.SCREEN);                             }                            }                      }b.resetMatrix(); //Stellt alle Befehle auf 0          // TYPO//Aktuelles Datumb.fill(black);    var  text = b.text(b.nf(b.day(),2)+"."              + b.nf(b.month(),2)+"."              + b.year()+", "               + b.nf(b.hour(),2)+":"               + b.nf(b.minute(),2) +" Uhr",              5, 5, 250, 30);    b.typo (text, "appliedFont", "Druk Wide\tMedium");    b.typo (text, "pointSize", 10);//Standpunkt    var text = b.text ("Finkenau – DMI – Hamburg", 5, b.height - 10, 250, 30);    b.typo (text, "appliedFont", "Druk Wide\tMedium");    b.typo (text, "pointSize", 10);//Spruch reinladen        //Texte laden        var text1 = b.loadString ("text1.txt");        var text2 = b.loadString ("text2.txt");        var text3 = b.loadString ("text3.txt");        //Array anlegen        var textArray = [text1,text2,text3];                //Zufallszahl auswürfeln für den Array        var randomtext = b.round (b.random(0,textArray.length-1));        //3. Textrahmen erstellen        b.fill (black);        var pointSizetext = 20        var textLeadingtext = 28        b.rotate (b.radians(270));            if(randomtext == 1){             // b.rotate (b.radians(270));              b.textLeading(textLeadingtext);              var textrahmen = b.text(text1, 5,3 * b.height/4,b.width-20,150);                    b.typo (textrahmen, "appliedFont", "Druk Wide\tBold");                b.typo (textrahmen, "pointSize", pointSizetext);            }            if(randomtext == 2){                // b.rotate (b.radians(270));                 b.textLeading(textLeadingtext);              var textrahmen = b.text(text1, 5,3 * b.height/4,b.width-20,150);                    b.typo (textrahmen, "appliedFont", "Druk Wide\tBold");                b.typo (textrahmen, "pointSize", pointSizetext);            }            if (randomtext == 3){               //  b.rotate (b.radians(270));                 b.textLeading(textLeadingtext);              var textrahmen = b.text(text1, 5, 3 * b.height/4,b.width-20,150);                  b.typo (textrahmen, "appliedFont", "Druk Wide\tBold");                b.typo (textrahmen, "pointSize", pointSizetext);                b.textLeading(100);            }  }  b.go();